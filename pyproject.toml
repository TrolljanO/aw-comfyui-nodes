[project]
name = "aw-comfyui-nodes"
version = "0.1.0"
description = "a|w ComfyUI custom nodes — Gemini Image Interactions API"
readme = "README.md"
license = { text = "Proprietary" }
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31",
    "python-dotenv>=1.0",
]

[tool.comfy]
PublisherId = "athie-wohnrath"
DisplayName = "a|w ComfyUI Nodes"
Icon = ""

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
